# k8s/secret-bws.yaml
# DO NOT store the actual token here.
# Create the secret out-of-band before deploying:
#
#   kubectl create secret generic bws-access-token \
#     --namespace=infra-brain \
#     --from-literal=BWS_ACCESS_TOKEN="$BWS_ACCESS_TOKEN"
#
# This secret is intentionally not managed by this repo.
