"""infra_brain.api — shared API scaffold package.

Provides:
  * api/schemas.py      — all Pydantic response-model classes
  * api/_helpers.py     — shared helper functions / constants
  * api/routers/        — per-domain route-handler modules (Task 3+)
"""
